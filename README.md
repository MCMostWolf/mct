# MCT

MCT is a toolkit to provide useful function for Minecraft hardcoded map translation, which is writen by Kotlin and support multiplatform.

The tool can help you extract text(in `TextCompound` form) from datapack and region file MCA,
and backfill the replacement.

